function ScoreBoard({ score, total }) {
  // =========================================================
  // TODO 8: Display the current score using the props.
  //
  //         The component receives 'score' and 'total' as props.
  //         Display them like: "Score: 3 / 10"
  //
  //         Hint:
  //         return (
  //           <div className="score-board">
  //             Score: {score} / {total}
  //           </div>
  //         );
  // =========================================================

  return <div className="score-board">Complete TODO 8 to show the score</div>;
}

export default ScoreBoard;
